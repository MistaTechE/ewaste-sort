Pulling full descriptions of items to be ewasted, using a list of tag numbers.
Updated to handle tags that are in the inventory but in a non-standard format
Updated to handle tags missing from the inventory

Goals: add logic that closes the window after processing reports, troubleshoot why it is so slow between buttons, make it a fully functioning app and share
